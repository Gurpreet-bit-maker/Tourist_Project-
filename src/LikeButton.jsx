import { useState } from "react";

export default function LikeButton() {
  let [islike, setLikeButton] = useState(false);

function btn()
{
    
}

  return (
    <div>
      <button onClick={btn}>clicked button</button>
    </div>
  );
}
