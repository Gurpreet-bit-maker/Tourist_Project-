
import ProductTab from "./ProductTab.jsx";

export default function Product() {
  
  return (
    <div style={styles}>
      <ProductTab title={0} description={0} idx={0} />
      <ProductTab title={1} description={1} idx={1} />
      <ProductTab title={2} description={2} idx={2} />
    </div>
  );
}
